{
    "name": "review_p",
    "version": "1.0.0",
    "scripts": {
        "client": "cd frontend && yarn start",
        "server": "java -jar openapi\\build\\libs\\openapi-0.0.1-SNAPSHOT.jar",
        "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
    },
    "dependencies": {
    },
    "devDependencies": {
        "concurrently": "^5.1.0"
    }
}